#include <metadata_parser.h>

void Metadata_Parser::get_metadata() {
//should be pased a file and determne if it has metadata and should return the metadata as well as the pointer to the file it was given.
}